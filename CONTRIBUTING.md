# 협업 규칙

## 이슈와 문서

이슈는 할 일과 완료 여부를 추적하고, 상세 설명은 [문서 안내](docs/README.md)의 정본 한 곳에서 관리한다.

- 구현 담당자가 할 일 한 문장, 완료 조건 1~3개와 참고 링크를 작성한다. 공동 결정은 결정할 질문과 필요한 확인을 적는다.
- PM은 제품 범위, 우선순위, 일정과 완료 조건을 확인한다. 다른 사람이 초안을 돕더라도 실제 담당자가 범위를 확인한다.
- 담당자에는 진행 책임을 맡은 사람을 지정하고, 필요한 협력자는 본문에 짧게 적는다. 공동 책임이 필요한 작업은 함께 지정하며 기존 담당을 임의로 바꾸지 않는다.
- 배경, 설계, 실행 방법과 긴 결과는 정본 문서에 쓰고 이슈에서는 링크한다. 이전 논의와 검증 근거는 보존한다.
- 미완료 작업은 문서에만 남기지 않는다. 기존 이슈를 먼저 연결하고, 따로 검토하고 끝낼 필요가 있는 작업만 분리한다.
- 주차가 바뀌어도 같은 작업의 이슈를 이어 쓴다. 주차별 진행은 Projects와 [주간 보고](docs/pm/weekly-operations.md)에서 묶는다.

### 분류와 진행 상태

- 제목에는 작업 내용을 쓴다. 분야는 `area:PM`, `area:FE`, `area:BE`, `area:AI`, `area:QA` 라벨로 구분하며 여러 분야가 관련되면 함께 붙인다.
- `bug`, `enhancement`, `documentation`, `question`은 작업 성격을 표시한다.
- 스프린트는 마일스톤, 주차와 목표일은 Projects에서 관리한다. 날짜는 합의된 일정만 사용한다. 보드가 준비되지 않았다면 목표일을 이슈 본문 한 줄로 적는다.
- 진행 상태는 할 일, 진행 중, 검토 중, 완료로 구분한다. 막힌 작업에는 이유와 선행 이슈를 연결한다.
- 스프린트 목표 이슈에는 사용자 흐름의 완료 조건과 작업 링크를 남긴다. 개별 작업의 세부 체크리스트를 복제하지 않는다.

### 기존 이슈 정리와 종료

1. 미완료 항목마다 계속 추적할 이슈를 먼저 연결한다.
2. 설명을 정본 문서에 반영한 뒤 이슈 본문을 줄인다. 문서 검토 중인 내용은 확정으로 표시하지 않는다.
3. 완료 조건을 충족하면 결과 PR과 검증 근거를 남기고 종료한다. 인계용 이슈를 닫을 때는 인계 완료와 기능 구현 완료를 구분한다.

## 파트 간 요청

- 작은 변경은 관련 담당자에게 필요한 화면이나 입력 예시와 함께 바로 문의한다. 합의가 필요한 변경을 한 영역에서 임의로 확정하지 않는다.
- 여러 영역의 구조나 제품 범위에 영향이 크면 관련 담당자와 논의한다. 합의한 결과는 계약과 문서 변경 PR으로 기록한다.

## 브랜치 구조

```text
main                 멘토 리뷰와 최종 통합, 직접 push 금지
└─ develop            기본 브랜치, 팀 내부 통합
   ├─ feature/*       기능 개발
   └─ refactor/*      멘토 피드백 반영
```

- 작업 브랜치는 최신 `develop`에서 생성한다.
- 기능 작업은 `feature/<작업명>`을 사용하고 멘토 피드백은 `refactor/<내용>`을 사용한다.
- Issue 번호는 브랜치 이름의 필수 조건이 아니다.
- PR 하나에는 하나의 목적만 포함한다.
- 공식 저장소 설정에 맞춰 Merge commit을 사용한다.
- 간단한 핫픽스와 작은 리팩터링은 공식 규정상 `develop`에 직접 push할 수 있지만, 추적이 필요한 변경은 PR을 사용한다.

## 팀 내부 PR

```text
feature/* 또는 refactor/*
→ develop PR
→ 같은 영역 팀원 1명 이상 리뷰
→ 리뷰 의견 반영
→ 주 PM 또는 부 PM이 develop에 머지
```

PR에는 다음 내용을 작성한다.

- 무엇을 왜 변경했는지
- 구체적으로 리뷰받고 싶은 파일, 함수 또는 설계
- 검토한 대안과 처리하지 않은 예외
- 실행한 테스트와 실제 결과
- 관련 문서와 데이터 계약 변경 여부

완료하는 이슈는 PR 본문에 `Closes #번호`로 연결한다. 기본 브랜치가 `develop`이므로 develop에 병합되면 해당 이슈가 자동 종료된다. 일부만 처리하거나 상위 목표를 참고할 때는 `관련: #번호`로 적고 `Closes`를 사용하지 않는다. `develop → main` PR은 포함된 결과를 요약하며 이미 끝난 이슈를 다시 완료 처리하지 않는다.

리뷰 수정은 원작성자가 반영하는 것을 기본으로 한다. 다른 팀원이 수정을 지원하면 원작성자가 변경과 검증 결과를 확인한다. 실제 실행, 합성값 검사와 미실행 범위를 구분한다.

작업은 검토 가능한 크기로 자주 통합한다. 여러 브랜치를 함께 확인해야 하면 로컬이나 임시 `integration/*` 브랜치에서 테스트하고, 통합 테스트 결과를 원래 작업 PR에 남긴다. 별도 합의 없이 상대 작업 브랜치에 변경을 push하지 않는다.

## 멘토 리뷰 PR

| 시점 | 작업 |
| --- | --- |
| 수요일 18시까지 | PM이 `develop → main` PR을 만들고 멘토를 리뷰어로 지정 |
| 수요일부터 토요일 | 멘토 피드백을 `refactor/*`에서 반영하고 팀 리뷰 후 `develop`에 머지 |
| 토요일 10시까지 | 반영 PR 링크와 변경 내역을 남기고 재리뷰 요청 |
| 일요일 | 멘토 승인 후 PM이 `main`에 머지 |

## 검토 책임

| 변경 영역 | 기술 검토 |
| --- | --- |
| `app/` | FE 담당 |
| `local_ai/` | AI 담당 |
| `backend/` | BE 담당 |
| `evals/` | QA 담당 |
| `docs/pm/` | 주 PM 또는 부 PM |
| `docs/legal/` | PM 확인 필수, 필요한 경우 외부 검토 |

여러 영역에 영향이 있으면 관련 담당자 모두에게 검토를 요청한다. 기술 검토가 끝난 뒤 주 PM 김영석 또는 부 PM 이예은이 병합을 관리한다.

## 문서 갱신

- 기술 선택 변경: 담당 영역 README
- 기술 구조 변경: 담당 README와 ADR
- 제품 범위 변경: `docs/pm/README.md`
- 공통 형식 변경: `docs/architecture/data-contracts.md`
- 개인정보 흐름 변경: `docs/legal/README.md`

구조 또는 계약을 변경하면서 관련 문서를 갱신하지 않은 PR은 병합하지 않는다.

## 데이터와 보안

- 실제 사용자 자료와 마스킹한 실제 자료를 커밋하지 않는다.
- 시크릿, 로컬 DB와 모델 파일을 커밋하지 않는다.
- 민감정보를 로그, 화면 캡처와 테스트 자료에 남기지 않는다.
- 데이터 이동 경로가 바뀌면 구현 전에 PM과 BE 검토를 받는다.
- 원본 음성과 이미지를 서버에서 처리하는 변경은 동의 상태 확인, 전송 암호화, ADR에 정한 삭제와 삭제 증명을 함께 구현한다.
- 외부 STT와 LLM을 도입하면 업체, 국가, 처리 항목, 보유기간, 자체 학습 여부와 법적 근거를 법률 문서에 반영한다.
